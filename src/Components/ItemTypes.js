/* the react-dnd library being used allows you to define multiple containers. Each container can be
configured to only accept a certain type of item being dropped. One item can have one or more types.
*/
export default {
    CARD: 'card',
}
